environments:
  - name: acceptance
    provider: eks
    cluster:
      maxSize: 1
  - name: production
    provider: eks
    cluster:
      maxSize: 1

service:
  name: ecogy-camunda-bpm
  port: 8080
  healthEndpoint: /
  pathPatterns:
    - /*
  pipeline:
    source:
      repo: ecogyenergy/docker-camunda-bpm-platform
